// import React from "react";
// // import { AdmindashboardCards } from "../../Data/Admindata1";
// const Home = () => {
//   return (
// {/* <div className="pt-20 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 px-20">
//   {AdmindashboardCards.map((card, id) => (
//     <div
//       key={id}
//       className="bg-white shadow-md rounded-2xl p-6 flex flex-col items-start gap-2 hover:shadow-lg transition duration-300"
//     >
//       <p className="text-gray-500 text-sm font-medium">ID: {card.id}</p>
//       <h2 className="text-xl font-semibold text-gray-800">{card.name}</h2>
//       <p className="text-2xl font-bold text-blue-600">{card.value}</p>
//       <span
//         className={`text-sm font-medium ${
//           card.trend >= 0 ? "text-green-500" : "text-red-500"
//         }`}
//       >
//         {card.trend >= 0 ? `▲ ${card.trend}%` : `▼ ${Math.abs(card.trend)}%`}
//       </span>
//     </div>
//   ))}
// </div> */}

// <h1>hlw</h1>
//   );
// };

// export default Home;
import React from "react";

const Home = () => {
  return (
    <div>
      <h1>this is home</h1>
    </div>
  );
};

export default Home;
